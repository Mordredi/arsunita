<div id="requests" class="container-lrg">
  <h2>Requests</h2>
  <div class="container">
    <% @requests.each do |request| %>
      <% friender = request.friender %>
      <div class="container-sm">
        <%= link_to "#{friender.full_name}", user_path(friender), :class => 'user-link' %>
        <%= link_to "Confirm Friend Request", friendship_path(request), :class => 'confirm' %>
      </div>
    <% end %>
  </div>
</div>